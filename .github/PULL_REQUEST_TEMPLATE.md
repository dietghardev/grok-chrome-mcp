## What this changes

<!-- and why -->

## Verification

- [ ] `npm test` passes
- [ ] `npm run smoke` passes
- [ ] New behaviour has a test that failed before the change
- [ ] If it touches `extension/`: reloaded at chrome://extensions and checked in a real browser
- [ ] If it adds a tool: registered in `index.ts`, listed in `test/smoke.ts`, documented in the README, and its permission level is stated
